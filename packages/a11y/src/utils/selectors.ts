import { A11Y_ATTRIBUTE, generateSelectors } from '@finsweet/attributes-utils';

import { ELEMENTS, SETTINGS } from './constants';

export const {} = generateSelectors(A11Y_ATTRIBUTE, ELEMENTS, SETTINGS);
