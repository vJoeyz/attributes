import { AUTO_VIDEO_ATTRIBUTE, generateSelectors } from '@finsweet/attributes-utils';

import { ELEMENTS, SETTINGS } from './constants';

export const {} = generateSelectors(AUTO_VIDEO_ATTRIBUTE, ELEMENTS, SETTINGS);
